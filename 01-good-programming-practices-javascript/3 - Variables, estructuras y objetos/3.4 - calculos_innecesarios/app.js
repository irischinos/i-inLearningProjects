// We can use more variables instead one only
// Calculating the length of each array element using "for"

var animals = ["cat", "dog", "fish", "dolphin"];

var animalsTotal = animals.length;

for(var i = 0; i < animalsTotal; i++) {
	console.log(animals[i]);
}